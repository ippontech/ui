styles-pages:
  stage: deploy
  interruptible: true
  pages:
    publish: styles/dist/
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  needs: ['styles-build']
  script:
    - echo "Preparing styles for GitLab Pages..."
