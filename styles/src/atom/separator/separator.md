## Separator

Thin dividing line rendered as an `hr` (implicit `separator` role). It stays flush so the surrounding component controls its spacing, and is reused to group content such as items inside a dropdown.
