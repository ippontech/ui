## Table

Cells and headers can shrink to their content width with `-minimal` (pass `{ minimal: true }` to the `ippon-table--header` and `ippon-table--cell` mixins).

A row can start with a row header: use `ippon-table--header` as the first element of a body row. Row headers are bold and follow the same border rules as cells.
