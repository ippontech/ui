## Grid

**Stretch**

A slot always occupies the full height of its row, but its content keeps its natural height. The
`-stretch` alternative on `ippon-grid--slot` makes that content fill the slot, so cards of a same
row end flush whatever their content.
