mixin ippon-dropdown(options)
  - const { id, ion, popover, additionalClass } = options || {};
  - const ionClass = `ippon-dropdown---${ion || 'buttons'}`;
  .ippon-dropdown(class=[ionClass, additionalClass], popover=popover || true, id=id)
    block
